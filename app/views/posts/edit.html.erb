<%= form_for @post, :html => { :multipart => true } do |f| %>
	<%= f.label :title %>:
  <%= f.text_field :title %><br />

  <%= f.label :body %>:
  <%= f.text_area :body %><br />

	<%= f.file_field :photo %>

  <%= f.submit %>
<% end %>

